syntax = "proto3";

enum Op {
    DISCOVERY = 0;
}

message Package {
    Message message = 1;
    string publicKey = 2;
    string signature = 3;
}

message Message {
    Op op = 1;
    bytes data = 2;
    string timeHash = 3;
}